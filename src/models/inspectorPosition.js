export default class InspectorPosition {
  constructor(lastPosition, fromDate, toDate, rowIndex) {
    this.lastPosition = lastPosition;
    this.fromDate = fromDate;
    this.toDate = toDate;
    this.rowIndex = rowIndex;
  }
}
